{
  "name": "mini-swap",
  "version": "1.0.0",
  "description": "A simplified DEX implementation for Polkadot EVM",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network local",
    "deploy:local": "hardhat run scripts/deploy.js --network local",
    "deploy:passet-hub": "hardhat run scripts/deploy.js --network passetHub",
    "node": "hardhat node",
    "ui": "cd ui && npm run dev",
    "ui:install": "cd ui && npm install"
  },
  "keywords": [
    "dex",
    "polkadot",
    "evm",
    "swap"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@parity/hardhat-polkadot": "^0.2.7",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
